import { Explore } from "@/components/explore/Explore";

/** /explore — PG-PUB-002 (Doc 04). Standalone Explore page. Cream environment (default). */
export default function ExplorePage() {
  return <Explore />;
}
